Spawning server: /opt/homebrew/bin/npx -y mcp-remote https://mcp.wirekitty.dev/mcp
Sending initialize...
Server response: {"jsonrpc":"2.0","id":1,"result":{"protocolVersion":"2024-11-05","capabilities":{"tools":{"listChanged":true}},"serverInfo":{"name":"wirekitty","version":"0.1.0"}}}
Sending notifications/initialized...
Calling tool: get_wireframe_schema...
Server response: {"jsonrpc":"2.0","id":2,"result":{"content":[{"type":"text","text":"# Wirekitty Schema\n\nA concise, code-like schema for describing wireframes that compiles to Wirekitty's visual format.\n\n## Calling create_wireframe (Important)\n\nUse exactly this input shape:\n```json\n{\n  \"schema\": {\n    \"screen\": \"Screen Name\",\n    \"children\": []\n  }\n}\n```\n\nIMPORTANT: Pass the wireframe schema as a JSON object or array only.\n\n`screen` is top-level only. Do not place a `screen` object inside any `children` array.\n\nFor multiple screens, set `schema` to an array of screen objects:\n```json\n{\n  \"schema\": [\n    { \"screen\": \"Step 1\", \"children\": [{ \"heading\": \"Welcome\" }] },\n    { \"screen\": \"Step 2\", \"children\": [{ \"heading\": \"Preferences\" }] }\n  ]\n}\n```\n\nOptional agent-session controls:\n```json\n{\n  \"schema\": { \"screen\": \"Screen Name\", \"children\": [] },\n  \"updateWireframeId\": \"uuid-for-in-place-update\"\n}\n```\n\nWhen `updateWireframeId` is provided, update in place:\n- Do not share a new preview link.\n- Do not reopen the preview (no `open`/`xdg-open`/`start`/openSimpleBrowser on updates).\n- Do not call `get_screenshot` immediately after `create_wireframe` unless the user asks for it or it is required to fulfill their request.\n\nDo not put `updateWireframeId` inside `schema` or inside any `children` array.\nInvalid:\n```json\n{\n  \"schema\": {\n    \"screen\": \"Screen Name\",\n    \"children\": [{ \"text\": \"Hello\" }, \"updateWireframeId\", \"uuid-for-in-place-update\"]\n  }\n}\n```\n\n## Critical Shape Rules\n\n- `schema` must be either one screen object or an array of screen objects.\n- Every item in every `children` array must be a single element object.\n- Every element object must contain exactly one primary element key.\n- Container arrays belong on the container key, not directly in `children`.\n- `screen` is reserved for top-level pages and views and must not appear inside `children`.\n\nValid:\n```javascript\nchildren: [\n  { heading: \"Welcome\" },\n  { card: [{ text: \"Start here\" }, { button: \"Continue\" }] },\n  { stack: { gap: 16, children: [{ label: \"Email\" }, { input: \"you@example.com\" }] } }\n]\n```\n\nInvalid:\n```javascript\nchildren: [\n  [{ heading: \"Welcome\" }],\n  { stack: true, children: [{ text: \"Welcome\" }] },\n  { row: true, children: [{ button: \"Back\" }, { button: \"Next\" }] },\n  \"Welcome\",\n  null\n]\n```\n\nIf you want a container, wrap it as an element object such as `{ stack: [...] }`, `{ row: [...] }`, `{ actions: [...] }`, `{ grid: { columns: 2, children: [...] } }`, or `{ card: [...] }`.\nCommon corrections:\n```javascript\n// Invalid legacy marker form:\n{ stack: true, gap: 16, children: [{ text: \"Welcome\" }] }\n\n// Valid:\n{ stack: { gap: 16, children: [{ text: \"Welcome\" }] } }\n\n// Invalid misplaced card options:\n{ card: [{ text: \"Hello\" }], padding: 24, border: true }\n\n// Valid:\n{ card: { padding: 24, border: true, children: [{ text: \"Hello\" }] } }\n\n// Invalid nested screen:\n{ screen: \"Filters\", children: [{ input: \"Search...\" }] }\n\n// Valid subsection inside a screen:\n{ card: [{ heading: \"Filters\" }, { input: \"Search...\" }] }\n```\nDo not put `children`, `gap`, `align`, or `justify` next to a container key such as `stack` or `row`; those settings belong inside the container value.\nDo not combine multiple element shortcuts in one object such as `{ text: \"A\", button: \"B\" }`; split them into separate elements.\n\n## Wireframe Intent\n\nThis is a wireframe. Keep the experience simple and avoid adding elements that are not necessary.\nPrioritize the core features and give important content room to breathe.\n\n## Basic Structure\n\n```javascript\n{\n  screen: \"Screen Name\",      // Required: name of the screen/page\n  style: \"floating\",          // Optional: \"floating\" (default), \"sectioned\", or \"flat\"\n  width: 1440,                // Optional: defaults to 1440\n  height: 900,                // Optional: defaults to 900\n  padding: 40,                // Optional: defaults to 40 (mobile often uses ~12-20; use 0 only for intentional edge-to-edge)\n  children: [...]             // Required: array of elements\n}\n```\n\nFor multiple screens, use an array:\n```javascript\n[\n  { screen: \"Login\", children: [...] },\n  { screen: \"Dashboard\", children: [...] }\n]\n```\n\n`screen` is reserved for top-level pages and views. Do not nest `screen` inside `children` or other screens; use layout containers such as `stack`, `row`, `grid`, `card`, `navbar`, `sidebar`, or `footer` instead.\n\n### Screen Style\nEach screen has an optional `style` property that controls the visual treatment:\n\n- `\"floating\"` (default): Rounded cards with borders floating on white background. Best for: landing pages, marketing sites, content-heavy pages.\n- `\"sectioned\"`: Square cards on a gray screen background. Best for: dashboards, admin panels, data-dense UIs. Keep normal screen padding by default; set `padding: 0` for full edge-to-edge content.\n- `\"flat\"`: No card backgrounds or borders, clean minimal look. Best for: text-heavy pages, documentation, settings pages.\n\nChoose the style that best matches the type of UI being built.\n\n```javascript\n{ screen: \"Dashboard\", style: \"sectioned\", children: [...] }\n{ screen: \"Landing Page\", style: \"floating\", children: [...] }\n{ screen: \"Settings\", style: \"flat\", children: [...] }\n```\n\n## Layout Containers\n\n### Stack (Vertical)\n```javascript\n{ stack: [...] }                           // Default gap: 16px\n{ stack: { gap: 24, children: [...] } }    // Custom gap\n{ stack: { gap: 16, align: \"center\", children: [...] } }  // Center children inside content width\n{ stack: { width: 320, height: 240, children: [...] } }   // Optional explicit container size\n{ stack: { widthMode: \"stretch\", children: [...] } }      // Fill available width\n{ stack: { widthMode: \"content\", padding: 12, children: [...] } } // Hug content with padding\n```\nStack `widthMode` options: `\"content\"` (default) or `\"stretch\"`.\n`align` controls how children are aligned inside the stack.\nOptional `width` reserves a container width; optional `height` reserves a minimum container height.\n\n### Row (Horizontal)\n```javascript\n{ row: [...] }                             // Default gap: 12px\n{ row: { gap: 8, children: [...] } }       // Custom gap\n{ row: { justify: \"space-between\", children: [...] } }  // Spaced\n{ row: { width: 480, height: 96, children: [...] } }    // Optional explicit container size\n{ row: { wrap: \"auto\", children: [...] } } // Wrap to a new line only if needed\n```\nJustify options: \"start\", \"center\", \"end\", \"space-between\", \"space-around\"\nWrap options: `true`, `false`, or `\"auto\"`\nOptional `width` constrains and reserves row width; optional `height` reserves a minimum row height.\nIMPORTANT: Child content will expand to fill available row space. Keep child `width` undefined for automatic sizing (most common), and set child `width` explicitly only when a region should remain fixed width.\nCards in the same `row` match the tallest card height by default unless a card sets an explicit `height`.\nRows made entirely of buttons automatically wrap on overflow as a safety fallback.\n\n### Actions (Responsive button/action row)\n```javascript\n{ actions: [{ button: \"Cancel\", variant: \"secondary\" }, { button: \"Save\", variant: \"primary\" }] }\n{ actions: { justify: \"end\", children: [{ button: \"Back\" }, { button: \"Continue\", variant: \"primary\" }] } }\n```\nUse `actions` instead of raw `row` for button bars, modal footers, and toolbar clusters.\nIt defaults to tighter spacing, prefers smaller buttons, wraps automatically, and stacks full-width on narrow/mobile layouts.\n\n### Grid\n```javascript\n{ grid: { columns: 3, gap: 24, children: [...] } }\n```\nNote: Grid items are sized from available width and column count. Use explicit `width` on items/containers when fixed widths are required.\n\n### Card (Container with padding and border)\n```javascript\n{ card: [...] }                            // Default padding: 24px\n{ card: { padding: 32, width: 400, children: [...] } }\n{ card: { border: false, children: [...] } } // Optional border toggle (default: true)\n{ card: { border: \"square\", children: [...] } } // Square corners with border\n```\n\n### Spacing Guidance\n- Use `gap` to separate sibling elements in `stack` and `row` if needed.\n- In `grid`, `gap` controls column spacing and is also the default row spacing.\n- Use `rowGap` in `grid` when vertical spacing between rows should differ from column spacing.\n- `gap` and `rowGap` are optional. Omitted values use layout defaults.\n- Do: omit `width` to use full available container width (most common).\n- Don’t: set fixed `width` unless you intentionally need a constrained region.\n\n## Text Elements\n\n```javascript\n{ text: \"Hello world\" }\n{ text: \"Muted text\", color: \"muted\" }\n{ text: \"Bold text\", bold: true }\n{ text: \"Large text\", size: \"lg\" }         // xs, sm, md, lg, xl, 2xl\n{ text: \"Centered text\", align: \"center\" }  // left, center, right\n\n{ heading: \"Page Title\" }                   // Large, bold\n{ heading: \"Subtitle\", level: 3 }           // H1-H3\n{ heading: { text: \"Title\", level: 3 } }    // Alternate object form\n{ heading: { text: \"Centered heading\", level: 2, align: \"center\" } }\n\n{ label: \"Email\" }                          // Small, for form labels\n{ label: \"Centered label\", align: \"center\" }\n\n{ link: \"Forgot password?\" }                // Underlined link text\n{ link: { text: \"Learn more\", url: \"#\" } }\n{ text: \"Inline link\", link: true }         // Alternate link style\n{ text: \"Centered link\", link: true, align: \"center\" } // Use text+link when alignment is needed\n```\n\nText in the schema is exported as bounded text. Use `align` for text alignment when you need centered or right-aligned copy.\nFor predictable paragraph wrapping, place text inside a container with explicit `width`.\n\n## Buttons\n\n```javascript\n{ button: \"Click me\" }                      // Default style\n{ button: \"Submit\", variant: \"primary\" }    // Purple fill\n{ button: \"Cancel\", variant: \"secondary\" }  // Gray fill\n{ button: \"Delete\", variant: \"outline\" }    // Border only\n{ button: \"Filter\", size: \"sm\" }            // Compact button\n{ button: \"Submit\", fullWidth: true }       // Full container width (in rows, fullWidth siblings share row width)\n{ button: { label: \"Add\", icon: \"plus\", size: \"sm\" } }  // Label + icon\n{ button: { icon: \"plus\" } }                // Icon-only button\n{ button: \"OK\", minWidth: 96 }              // Optional width floor\n```\nButton size options: `\"md\"` (default) or `\"sm\"`.\nButtons size to content by default.\nPrefer `size: \"sm\"` for dense action rows, secondary toolbar actions, and narrow side panels.\n\n## Input Elements\n\n```javascript\n{ input: \"Email\" }                          // Placeholder text\n{ input: \"Password\", type: \"password\" }     // Shows dots\n{ input: { placeholder: \"you@example.com\" } }\n\n{ textarea: \"Enter description...\" }\n{ textarea: { placeholder: \"Notes\", height: 150 } }\n\n{ dropdown: [\"Option 1\", \"Option 2\", \"Option 3\"] }\n{ dropdown: { label: \"Select country\", options: [\"US\", \"UK\", \"CA\"] } }\n{ dropdown: [\"A\", \"B\", \"C\"], expanded: true }  // Show open menu\n\n{ searchInput: \"Search...\" }                  // Search input with icon\n{ searchInput: { placeholder: \"Search users...\" } }\n{ searchInput: { placeholder: \"Search users...\", width: 300 } } // Optional constrained width\n```\nKeep `type` next to `input`, not inside the `input` object.\n\nFor input-like primitives, omit `width` to fill the available container width. Add `width` only when you intentionally want a narrower control.\n\n## Form Controls\n\n```javascript\n{ checkbox: \"Remember me\" }\n{ checkbox: \"Agree to terms\", checked: false }  // Unchecked\n{ checkbox: { label: \"Receive updates\", checked: true } }\n\n{ checkboxCard: \"Starter plan\" }\n{ checkboxCard: { label: \"Pro plan\", checked: true, width: 360, height: 72 } }\n\n{ radio: \"Option A\" }\n{ radio: \"Option B\", selected: false }\n{ radio: { label: \"Option C\", selected: true } }\n\n{ switch: \"Dark mode\" }\n{ switch: \"Notifications\", on: false }\n{ switch: { label: \"Auto-save\", on: true } }\n\n{ slider: 50 }                              // Value 0-100\n{ slider: 75 }\n{ slider: 75, width: 300 }                  // Optional constrained width\n\n{ progress: 65 }                            // Value 0-100\n{ progress: { value: 80, label: \"80% complete\" } }\n```\n\n## Complex Elements\n\n### Modal\nModals are overlays, not ordinary in-flow cards. To show an open modal, keep the underlying screen content in the screen and add the modal as a direct child of the screen so it sits above the rest of the UI. Put the dialog body inside `modal.children`. `children` is required; `title` is optional.\nIf `width` and/or `height` are omitted, the modal auto-sizes to content with a minimum size of 400x250.\n```javascript\n{\n  screen: \"Settings\",\n  children: [\n    {\n      card: [\n        { heading: \"Billing\" },\n        { text: \"Your current plan renews on April 1.\" },\n        { button: \"Change plan\", variant: \"primary\" }\n      ]\n    },\n    { modal: {\n      title: \"Invite Team Member\",\n      children: [\n        { callout: { text: \"Invitees will receive an email with workspace access instructions.\", variant: \"info\" } },\n        { input: \"alex@company.com\" },\n        { dropdown: { label: \"Workspace\", options: [\"Marketing\", \"Product\", \"Sales\"] } },\n        { dropdown: { label: \"Role\", options: [\"Editor\", \"Admin\", \"Viewer\"] } },\n        { divider: true },\n        { actions: { justify: \"end\", children: [\n          { button: \"Cancel\", variant: \"secondary\" },\n          { button: \"Send Invite\", variant: \"primary\" }\n        ]}}\n      ]\n    }}\n  ]\n}\n```\n\n### Table\n```javascript\n{ table: { columns: 4, rows: 5 } }\n{ table: { columns: [\"Name\", \"Email\", \"Role\"], rows: 10 } }\n```\n\n### Tabs\n```javascript\n{ tabs: [\"Overview\", \"Settings\", \"Help\"] }\n{ tabs: [{ label: \"Active\", active: true }, \"Inactive\"] }\n{ tabs: { items: [\"Overview\", \"Settings\", \"Help\"] } } // Content-width tabs (default)\n{ tabs: { items: [\"Overview\", \"Settings\", \"Help\"], widthMode: \"stretch\" } } // Fill available width\n{ tabs: { items: [\"Overview\", \"Settings\", \"Help\"], align: \"start\" } } // start | center | end (content mode)\n```\nBy default, tabs size to their labels (`widthMode: \"content\"`). Use `widthMode: \"stretch\"` only when equal-width tabs should fill the available container width.\n\n### Calendar\n```javascript\n{ calendar: true }\n{ calendar: { height: 220 } }              // Fills available width by default\n{ calendar: { width: 320, height: 220 } }  // Optional constrained size\n```\n\n### Map\n```javascript\n{ map: true }\n{ map: { height: 220 } }                   // Fills available width by default\n{ map: { width: 320, height: 220 } }       // Optional constrained size\n```\n\n## Composite Elements (Syntax Only)\n\nThese primitives are optional and can be used in any composition.\n\n### List\n```javascript\n{ list: { count: 5, template: { text: \"...\" } } }\n{ list: { count: 5, gap: 12, dividers: true, template: { text: \"...\" } } }\n```\n\n### Navbar\nIn `sectioned` style (and in mobile-width screens), navbars default to full-width and align flush to the top/left screen edges.\nIn `floating` desktop screens, navbars stay inside screen padding unless you explicitly set a full-width `width`.\n```javascript\n{ navbar: { items: [\"Item A\", \"Item B\"] } }\n{ navbar: {\n  logo: \"Logo\",\n  items: [{ label: \"Item A\", active: true }, \"Item B\"],\n  actions: [{ button: \"Action\" }],\n  width: 1200,\n  height: 64,\n  padding: 16\n}}\n```\n\n### Footer\nIn `sectioned` style (and in mobile-width screens), footers default to full-width and align flush to the screen's left/right edges and bottom edge.\nIn `floating` desktop screens, footers stay inside screen padding unless you explicitly set a full-width `width`.\nUse `logo` for optional brand text on the left, and `copyright` for muted legal text.\n`links` is the canonical footer navigation key. Do not use `items` here.\n```javascript\n{ footer: { logo: \"Acme\", copyright: \"© 2026 Acme\", links: [\"Privacy\", \"Terms\"] } }\n{ footer: {\n  logo: \"Acme\",\n  copyright: \"© 2026 Acme, Inc.\",\n  links: [{ label: \"Privacy\", active: true }, \"Terms\", \"Contact\"],\n  actions: [{ button: \"Back to top\", variant: \"secondary\" }],\n  width: 1200,\n  height: 64,\n  padding: 16\n}}\n```\n\n### Sidebar\nIn `sectioned` style, sidebars align flush to the screen's left edge. In other styles, they follow screen padding.\n```javascript\n{ sidebar: { items: [{ label: \"Item A\" }] } }\n{ sidebar: { title: \"Settings\", items: [\"Profile\", \"Security\", \"Billing\"] } }\n{ sidebar: {\n  width: 240,\n  padding: 20,\n  title: \"Title\",\n  items: [\n    { label: \"Item A\", icon: \"home\", active: true },\n    { label: \"Item B\", icon: \"star\" }\n  ]\n}}\n```\n\n### Breadcrumbs\n```javascript\n{ breadcrumbs: [\"Level 1\", \"Level 2\", \"Level 3\"] }\n```\n\n### Pagination\n```javascript\n{ pagination: { pages: 10 } }\n{ pagination: { items: [\"1\", \"2\", \"...\", \"10\"] } }\n```\n\n### Chart / Bar Chart\n```javascript\n{ chart: true }\n{ chart: { type: \"line\", bars: 6, height: 200 } }   // Fills available width by default\n{ chart: { type: \"line\", bars: 6, width: 300, height: 200 } } // Optional constrained size\n{ chart: { type: \"pie\", title: \"Revenue Split\", showTitle: true } }\n{ barchart: { type: \"donut\", xAxisLabel: \"Quarter\", showXAxisLabel: true } }\n```\n\n### Accordion\n```javascript\n{ accordion: [\n  { title: \"Item A\", expanded: true, children: [{ text: \"...\" }] },\n  { title: \"Item B\" }\n]}\n```\n\n### Stepper\n```javascript\n{ stepper: { stepCount: 4, activeStep: 2 } }      // Active step is 1-based\n{ stepper: { stepCount: 3, current: 1 } }          // current is 0-based\n```\n\n### Key/Value List\n```javascript\n{ keyValueList: [\n  { key: \"Status\", value: \"Active\" },\n  { key: \"Plan\", value: \"Pro\" }\n] }\n{ keyValueList: {\n  entries: [{ key: \"Name\", value: \"Ada\" }, { key: \"Role\", value: \"Admin\" }],\n  rowHeight: 36,\n  keyAlign: \"left\",\n  valueAlign: \"right\",\n  borderMode: \"full\"\n} }\n{ keyValueList: {\n  entries: [{ key: \"Name\", value: \"Ada\" }, { key: \"Role\", value: \"Admin\" }],\n  width: 360,\n  rowHeight: 36,\n  keyAlign: \"left\",\n  valueAlign: \"right\",\n  borderMode: \"full\"\n} } // Optional constrained width\n```\n\n### Stat Card\n```javascript\n{ stat: { value: \"2,847\", label: \"Active Users\", trend: \"+12%\" } }\n{ stat: { value: \"$184K\", label: \"Monthly Revenue\", trend: \"-3.2%\" } }\n{ stat: { value: \"$184K\", label: \"Monthly Revenue\", trend: \"-3.2%\" }, width: 260 } // Optional constrained width\n{ stat: \"2,847\" } // shorthand value-only metric\n```\n\n### Avatar Group\n```javascript\n{ avatarGroup: { count: 3, size: 32, overlap: 10 } }\n```\n\n## Visual Elements\n\n```javascript\n{ image: true }                             // Default size 200x150\n{ image: { height: 200 } }                 // Fills available width by default\n{ image: { width: 300, height: 200 } }     // Optional constrained size\n\n{ avatar: 48 }                              // Circular, 48px\n\n{ icon: \"star\" }                            // Lucide icon name\n{ icon: \"arrow-right\", size: 32 }\n{ icon: \"brain\", size: 40, color: \"primary\" }\n\n{ badge: \"New\" }\n{ badge: \"Error\", variant: \"outline\" }\n\n{ divider: true }                           // Horizontal line\n{ divider: { orientation: \"horizontal\" } } // Fills available width by default\n{ divider: { orientation: \"horizontal\", width: 320 } } // Optional constrained width\n{ divider: { orientation: \"vertical\", height: 120 } }\n\n{ callout: \"Important information\" }\n{ callout: \"Warning message\", variant: \"warning\" }  // info, warning, error, success\n\n{ tooltip: \"Helpful hint\" }\n\n{ rectangle: true }                         // 150x100 default\n{ rectangle: { height: 100, fill: \"#f0f0f0\" } }       // Fills available width by default\n{ rectangle: { width: 200, height: 100, fill: \"#f0f0f0\" } } // Optional constrained width\n\n{ skeleton: \"text\" }                        // Presets: text, avatar, card\n{ skeleton: { height: 20 } }               // Fills available width by default\n{ skeleton: { width: 200, height: 20 } }\n\n{ spinner: true }                           // Loader icon\n{ spinner: { size: 32 } }\n```\n\nDo not nest icon options inside the icon value.\nValid:\n```javascript\n{ icon: \"brain\", size: 40, color: \"primary\" }\n```\nInvalid:\n```javascript\n{ icon: { icon: \"brain\", size: 40, color: \"primary\" } }\n```\n"}]}}
Tool call completed successfully.
RESULT_JSON:{"content":[{"type":"text","text":"# Wirekitty Schema\n\nA concise, code-like schema for describing wireframes that compiles to Wirekitty's visual format.\n\n## Calling create_wireframe (Important)\n\nUse exactly this input shape:\n```json\n{\n  \"schema\": {\n    \"screen\": \"Screen Name\",\n    \"children\": []\n  }\n}\n```\n\nIMPORTANT: Pass the wireframe schema as a JSON object or array only.\n\n`screen` is top-level only. Do not place a `screen` object inside any `children` array.\n\nFor multiple screens, set `schema` to an array of screen objects:\n```json\n{\n  \"schema\": [\n    { \"screen\": \"Step 1\", \"children\": [{ \"heading\": \"Welcome\" }] },\n    { \"screen\": \"Step 2\", \"children\": [{ \"heading\": \"Preferences\" }] }\n  ]\n}\n```\n\nOptional agent-session controls:\n```json\n{\n  \"schema\": { \"screen\": \"Screen Name\", \"children\": [] },\n  \"updateWireframeId\": \"uuid-for-in-place-update\"\n}\n```\n\nWhen `updateWireframeId` is provided, update in place:\n- Do not share a new preview link.\n- Do not reopen the preview (no `open`/`xdg-open`/`start`/openSimpleBrowser on updates).\n- Do not call `get_screenshot` immediately after `create_wireframe` unless the user asks for it or it is required to fulfill their request.\n\nDo not put `updateWireframeId` inside `schema` or inside any `children` array.\nInvalid:\n```json\n{\n  \"schema\": {\n    \"screen\": \"Screen Name\",\n    \"children\": [{ \"text\": \"Hello\" }, \"updateWireframeId\", \"uuid-for-in-place-update\"]\n  }\n}\n```\n\n## Critical Shape Rules\n\n- `schema` must be either one screen object or an array of screen objects.\n- Every item in every `children` array must be a single element object.\n- Every element object must contain exactly one primary element key.\n- Container arrays belong on the container key, not directly in `children`.\n- `screen` is reserved for top-level pages and views and must not appear inside `children`.\n\nValid:\n```javascript\nchildren: [\n  { heading: \"Welcome\" },\n  { card: [{ text: \"Start here\" }, { button: \"Continue\" }] },\n  { stack: { gap: 16, children: [{ label: \"Email\" }, { input: \"you@example.com\" }] } }\n]\n```\n\nInvalid:\n```javascript\nchildren: [\n  [{ heading: \"Welcome\" }],\n  { stack: true, children: [{ text: \"Welcome\" }] },\n  { row: true, children: [{ button: \"Back\" }, { button: \"Next\" }] },\n  \"Welcome\",\n  null\n]\n```\n\nIf you want a container, wrap it as an element object such as `{ stack: [...] }`, `{ row: [...] }`, `{ actions: [...] }`, `{ grid: { columns: 2, children: [...] } }`, or `{ card: [...] }`.\nCommon corrections:\n```javascript\n// Invalid legacy marker form:\n{ stack: true, gap: 16, children: [{ text: \"Welcome\" }] }\n\n// Valid:\n{ stack: { gap: 16, children: [{ text: \"Welcome\" }] } }\n\n// Invalid misplaced card options:\n{ card: [{ text: \"Hello\" }], padding: 24, border: true }\n\n// Valid:\n{ card: { padding: 24, border: true, children: [{ text: \"Hello\" }] } }\n\n// Invalid nested screen:\n{ screen: \"Filters\", children: [{ input: \"Search...\" }] }\n\n// Valid subsection inside a screen:\n{ card: [{ heading: \"Filters\" }, { input: \"Search...\" }] }\n```\nDo not put `children`, `gap`, `align`, or `justify` next to a container key such as `stack` or `row`; those settings belong inside the container value.\nDo not combine multiple element shortcuts in one object such as `{ text: \"A\", button: \"B\" }`; split them into separate elements.\n\n## Wireframe Intent\n\nThis is a wireframe. Keep the experience simple and avoid adding elements that are not necessary.\nPrioritize the core features and give important content room to breathe.\n\n## Basic Structure\n\n```javascript\n{\n  screen: \"Screen Name\",      // Required: name of the screen/page\n  style: \"floating\",          // Optional: \"floating\" (default), \"sectioned\", or \"flat\"\n  width: 1440,                // Optional: defaults to 1440\n  height: 900,                // Optional: defaults to 900\n  padding: 40,                // Optional: defaults to 40 (mobile often uses ~12-20; use 0 only for intentional edge-to-edge)\n  children: [...]             // Required: array of elements\n}\n```\n\nFor multiple screens, use an array:\n```javascript\n[\n  { screen: \"Login\", children: [...] },\n  { screen: \"Dashboard\", children: [...] }\n]\n```\n\n`screen` is reserved for top-level pages and views. Do not nest `screen` inside `children` or other screens; use layout containers such as `stack`, `row`, `grid`, `card`, `navbar`, `sidebar`, or `footer` instead.\n\n### Screen Style\nEach screen has an optional `style` property that controls the visual treatment:\n\n- `\"floating\"` (default): Rounded cards with borders floating on white background. Best for: landing pages, marketing sites, content-heavy pages.\n- `\"sectioned\"`: Square cards on a gray screen background. Best for: dashboards, admin panels, data-dense UIs. Keep normal screen padding by default; set `padding: 0` for full edge-to-edge content.\n- `\"flat\"`: No card backgrounds or borders, clean minimal look. Best for: text-heavy pages, documentation, settings pages.\n\nChoose the style that best matches the type of UI being built.\n\n```javascript\n{ screen: \"Dashboard\", style: \"sectioned\", children: [...] }\n{ screen: \"Landing Page\", style: \"floating\", children: [...] }\n{ screen: \"Settings\", style: \"flat\", children: [...] }\n```\n\n## Layout Containers\n\n### Stack (Vertical)\n```javascript\n{ stack: [...] }                           // Default gap: 16px\n{ stack: { gap: 24, children: [...] } }    // Custom gap\n{ stack: { gap: 16, align: \"center\", children: [...] } }  // Center children inside content width\n{ stack: { width: 320, height: 240, children: [...] } }   // Optional explicit container size\n{ stack: { widthMode: \"stretch\", children: [...] } }      // Fill available width\n{ stack: { widthMode: \"content\", padding: 12, children: [...] } } // Hug content with padding\n```\nStack `widthMode` options: `\"content\"` (default) or `\"stretch\"`.\n`align` controls how children are aligned inside the stack.\nOptional `width` reserves a container width; optional `height` reserves a minimum container height.\n\n### Row (Horizontal)\n```javascript\n{ row: [...] }                             // Default gap: 12px\n{ row: { gap: 8, children: [...] } }       // Custom gap\n{ row: { justify: \"space-between\", children: [...] } }  // Spaced\n{ row: { width: 480, height: 96, children: [...] } }    // Optional explicit container size\n{ row: { wrap: \"auto\", children: [...] } } // Wrap to a new line only if needed\n```\nJustify options: \"start\", \"center\", \"end\", \"space-between\", \"space-around\"\nWrap options: `true`, `false`, or `\"auto\"`\nOptional `width` constrains and reserves row width; optional `height` reserves a minimum row height.\nIMPORTANT: Child content will expand to fill available row space. Keep child `width` undefined for automatic sizing (most common), and set child `width` explicitly only when a region should remain fixed width.\nCards in the same `row` match the tallest card height by default unless a card sets an explicit `height`.\nRows made entirely of buttons automatically wrap on overflow as a safety fallback.\n\n### Actions (Responsive button/action row)\n```javascript\n{ actions: [{ button: \"Cancel\", variant: \"secondary\" }, { button: \"Save\", variant: \"primary\" }] }\n{ actions: { justify: \"end\", children: [{ button: \"Back\" }, { button: \"Continue\", variant: \"primary\" }] } }\n```\nUse `actions` instead of raw `row` for button bars, modal footers, and toolbar clusters.\nIt defaults to tighter spacing, prefers smaller buttons, wraps automatically, and stacks full-width on narrow/mobile layouts.\n\n### Grid\n```javascript\n{ grid: { columns: 3, gap: 24, children: [...] } }\n```\nNote: Grid items are sized from available width and column count. Use explicit `width` on items/containers when fixed widths are required.\n\n### Card (Container with padding and border)\n```javascript\n{ card: [...] }                            // Default padding: 24px\n{ card: { padding: 32, width: 400, children: [...] } }\n{ card: { border: false, children: [...] } } // Optional border toggle (default: true)\n{ card: { border: \"square\", children: [...] } } // Square corners with border\n```\n\n### Spacing Guidance\n- Use `gap` to separate sibling elements in `stack` and `row` if needed.\n- In `grid`, `gap` controls column spacing and is also the default row spacing.\n- Use `rowGap` in `grid` when vertical spacing between rows should differ from column spacing.\n- `gap` and `rowGap` are optional. Omitted values use layout defaults.\n- Do: omit `width` to use full available container width (most common).\n- Don’t: set fixed `width` unless you intentionally need a constrained region.\n\n## Text Elements\n\n```javascript\n{ text: \"Hello world\" }\n{ text: \"Muted text\", color: \"muted\" }\n{ text: \"Bold text\", bold: true }\n{ text: \"Large text\", size: \"lg\" }         // xs, sm, md, lg, xl, 2xl\n{ text: \"Centered text\", align: \"center\" }  // left, center, right\n\n{ heading: \"Page Title\" }                   // Large, bold\n{ heading: \"Subtitle\", level: 3 }           // H1-H3\n{ heading: { text: \"Title\", level: 3 } }    // Alternate object form\n{ heading: { text: \"Centered heading\", level: 2, align: \"center\" } }\n\n{ label: \"Email\" }                          // Small, for form labels\n{ label: \"Centered label\", align: \"center\" }\n\n{ link: \"Forgot password?\" }                // Underlined link text\n{ link: { text: \"Learn more\", url: \"#\" } }\n{ text: \"Inline link\", link: true }         // Alternate link style\n{ text: \"Centered link\", link: true, align: \"center\" } // Use text+link when alignment is needed\n```\n\nText in the schema is exported as bounded text. Use `align` for text alignment when you need centered or right-aligned copy.\nFor predictable paragraph wrapping, place text inside a container with explicit `width`.\n\n## Buttons\n\n```javascript\n{ button: \"Click me\" }                      // Default style\n{ button: \"Submit\", variant: \"primary\" }    // Purple fill\n{ button: \"Cancel\", variant: \"secondary\" }  // Gray fill\n{ button: \"Delete\", variant: \"outline\" }    // Border only\n{ button: \"Filter\", size: \"sm\" }            // Compact button\n{ button: \"Submit\", fullWidth: true }       // Full container width (in rows, fullWidth siblings share row width)\n{ button: { label: \"Add\", icon: \"plus\", size: \"sm\" } }  // Label + icon\n{ button: { icon: \"plus\" } }                // Icon-only button\n{ button: \"OK\", minWidth: 96 }              // Optional width floor\n```\nButton size options: `\"md\"` (default) or `\"sm\"`.\nButtons size to content by default.\nPrefer `size: \"sm\"` for dense action rows, secondary toolbar actions, and narrow side panels.\n\n## Input Elements\n\n```javascript\n{ input: \"Email\" }                          // Placeholder text\n{ input: \"Password\", type: \"password\" }     // Shows dots\n{ input: { placeholder: \"you@example.com\" } }\n\n{ textarea: \"Enter description...\" }\n{ textarea: { placeholder: \"Notes\", height: 150 } }\n\n{ dropdown: [\"Option 1\", \"Option 2\", \"Option 3\"] }\n{ dropdown: { label: \"Select country\", options: [\"US\", \"UK\", \"CA\"] } }\n{ dropdown: [\"A\", \"B\", \"C\"], expanded: true }  // Show open menu\n\n{ searchInput: \"Search...\" }                  // Search input with icon\n{ searchInput: { placeholder: \"Search users...\" } }\n{ searchInput: { placeholder: \"Search users...\", width: 300 } } // Optional constrained width\n```\nKeep `type` next to `input`, not inside the `input` object.\n\nFor input-like primitives, omit `width` to fill the available container width. Add `width` only when you intentionally want a narrower control.\n\n## Form Controls\n\n```javascript\n{ checkbox: \"Remember me\" }\n{ checkbox: \"Agree to terms\", checked: false }  // Unchecked\n{ checkbox: { label: \"Receive updates\", checked: true } }\n\n{ checkboxCard: \"Starter plan\" }\n{ checkboxCard: { label: \"Pro plan\", checked: true, width: 360, height: 72 } }\n\n{ radio: \"Option A\" }\n{ radio: \"Option B\", selected: false }\n{ radio: { label: \"Option C\", selected: true } }\n\n{ switch: \"Dark mode\" }\n{ switch: \"Notifications\", on: false }\n{ switch: { label: \"Auto-save\", on: true } }\n\n{ slider: 50 }                              // Value 0-100\n{ slider: 75 }\n{ slider: 75, width: 300 }                  // Optional constrained width\n\n{ progress: 65 }                            // Value 0-100\n{ progress: { value: 80, label: \"80% complete\" } }\n```\n\n## Complex Elements\n\n### Modal\nModals are overlays, not ordinary in-flow cards. To show an open modal, keep the underlying screen content in the screen and add the modal as a direct child of the screen so it sits above the rest of the UI. Put the dialog body inside `modal.children`. `children` is required; `title` is optional.\nIf `width` and/or `height` are omitted, the modal auto-sizes to content with a minimum size of 400x250.\n```javascript\n{\n  screen: \"Settings\",\n  children: [\n    {\n      card: [\n        { heading: \"Billing\" },\n        { text: \"Your current plan renews on April 1.\" },\n        { button: \"Change plan\", variant: \"primary\" }\n      ]\n    },\n    { modal: {\n      title: \"Invite Team Member\",\n      children: [\n        { callout: { text: \"Invitees will receive an email with workspace access instructions.\", variant: \"info\" } },\n        { input: \"alex@company.com\" },\n        { dropdown: { label: \"Workspace\", options: [\"Marketing\", \"Product\", \"Sales\"] } },\n        { dropdown: { label: \"Role\", options: [\"Editor\", \"Admin\", \"Viewer\"] } },\n        { divider: true },\n        { actions: { justify: \"end\", children: [\n          { button: \"Cancel\", variant: \"secondary\" },\n          { button: \"Send Invite\", variant: \"primary\" }\n        ]}}\n      ]\n    }}\n  ]\n}\n```\n\n### Table\n```javascript\n{ table: { columns: 4, rows: 5 } }\n{ table: { columns: [\"Name\", \"Email\", \"Role\"], rows: 10 } }\n```\n\n### Tabs\n```javascript\n{ tabs: [\"Overview\", \"Settings\", \"Help\"] }\n{ tabs: [{ label: \"Active\", active: true }, \"Inactive\"] }\n{ tabs: { items: [\"Overview\", \"Settings\", \"Help\"] } } // Content-width tabs (default)\n{ tabs: { items: [\"Overview\", \"Settings\", \"Help\"], widthMode: \"stretch\" } } // Fill available width\n{ tabs: { items: [\"Overview\", \"Settings\", \"Help\"], align: \"start\" } } // start | center | end (content mode)\n```\nBy default, tabs size to their labels (`widthMode: \"content\"`). Use `widthMode: \"stretch\"` only when equal-width tabs should fill the available container width.\n\n### Calendar\n```javascript\n{ calendar: true }\n{ calendar: { height: 220 } }              // Fills available width by default\n{ calendar: { width: 320, height: 220 } }  // Optional constrained size\n```\n\n### Map\n```javascript\n{ map: true }\n{ map: { height: 220 } }                   // Fills available width by default\n{ map: { width: 320, height: 220 } }       // Optional constrained size\n```\n\n## Composite Elements (Syntax Only)\n\nThese primitives are optional and can be used in any composition.\n\n### List\n```javascript\n{ list: { count: 5, template: { text: \"...\" } } }\n{ list: { count: 5, gap: 12, dividers: true, template: { text: \"...\" } } }\n```\n\n### Navbar\nIn `sectioned` style (and in mobile-width screens), navbars default to full-width and align flush to the top/left screen edges.\nIn `floating` desktop screens, navbars stay inside screen padding unless you explicitly set a full-width `width`.\n```javascript\n{ navbar: { items: [\"Item A\", \"Item B\"] } }\n{ navbar: {\n  logo: \"Logo\",\n  items: [{ label: \"Item A\", active: true }, \"Item B\"],\n  actions: [{ button: \"Action\" }],\n  width: 1200,\n  height: 64,\n  padding: 16\n}}\n```\n\n### Footer\nIn `sectioned` style (and in mobile-width screens), footers default to full-width and align flush to the screen's left/right edges and bottom edge.\nIn `floating` desktop screens, footers stay inside screen padding unless you explicitly set a full-width `width`.\nUse `logo` for optional brand text on the left, and `copyright` for muted legal text.\n`links` is the canonical footer navigation key. Do not use `items` here.\n```javascript\n{ footer: { logo: \"Acme\", copyright: \"© 2026 Acme\", links: [\"Privacy\", \"Terms\"] } }\n{ footer: {\n  logo: \"Acme\",\n  copyright: \"© 2026 Acme, Inc.\",\n  links: [{ label: \"Privacy\", active: true }, \"Terms\", \"Contact\"],\n  actions: [{ button: \"Back to top\", variant: \"secondary\" }],\n  width: 1200,\n  height: 64,\n  padding: 16\n}}\n```\n\n### Sidebar\nIn `sectioned` style, sidebars align flush to the screen's left edge. In other styles, they follow screen padding.\n```javascript\n{ sidebar: { items: [{ label: \"Item A\" }] } }\n{ sidebar: { title: \"Settings\", items: [\"Profile\", \"Security\", \"Billing\"] } }\n{ sidebar: {\n  width: 240,\n  padding: 20,\n  title: \"Title\",\n  items: [\n    { label: \"Item A\", icon: \"home\", active: true },\n    { label: \"Item B\", icon: \"star\" }\n  ]\n}}\n```\n\n### Breadcrumbs\n```javascript\n{ breadcrumbs: [\"Level 1\", \"Level 2\", \"Level 3\"] }\n```\n\n### Pagination\n```javascript\n{ pagination: { pages: 10 } }\n{ pagination: { items: [\"1\", \"2\", \"...\", \"10\"] } }\n```\n\n### Chart / Bar Chart\n```javascript\n{ chart: true }\n{ chart: { type: \"line\", bars: 6, height: 200 } }   // Fills available width by default\n{ chart: { type: \"line\", bars: 6, width: 300, height: 200 } } // Optional constrained size\n{ chart: { type: \"pie\", title: \"Revenue Split\", showTitle: true } }\n{ barchart: { type: \"donut\", xAxisLabel: \"Quarter\", showXAxisLabel: true } }\n```\n\n### Accordion\n```javascript\n{ accordion: [\n  { title: \"Item A\", expanded: true, children: [{ text: \"...\" }] },\n  { title: \"Item B\" }\n]}\n```\n\n### Stepper\n```javascript\n{ stepper: { stepCount: 4, activeStep: 2 } }      // Active step is 1-based\n{ stepper: { stepCount: 3, current: 1 } }          // current is 0-based\n```\n\n### Key/Value List\n```javascript\n{ keyValueList: [\n  { key: \"Status\", value: \"Active\" },\n  { key: \"Plan\", value: \"Pro\" }\n] }\n{ keyValueList: {\n  entries: [{ key: \"Name\", value: \"Ada\" }, { key: \"Role\", value: \"Admin\" }],\n  rowHeight: 36,\n  keyAlign: \"left\",\n  valueAlign: \"right\",\n  borderMode: \"full\"\n} }\n{ keyValueList: {\n  entries: [{ key: \"Name\", value: \"Ada\" }, { key: \"Role\", value: \"Admin\" }],\n  width: 360,\n  rowHeight: 36,\n  keyAlign: \"left\",\n  valueAlign: \"right\",\n  borderMode: \"full\"\n} } // Optional constrained width\n```\n\n### Stat Card\n```javascript\n{ stat: { value: \"2,847\", label: \"Active Users\", trend: \"+12%\" } }\n{ stat: { value: \"$184K\", label: \"Monthly Revenue\", trend: \"-3.2%\" } }\n{ stat: { value: \"$184K\", label: \"Monthly Revenue\", trend: \"-3.2%\" }, width: 260 } // Optional constrained width\n{ stat: \"2,847\" } // shorthand value-only metric\n```\n\n### Avatar Group\n```javascript\n{ avatarGroup: { count: 3, size: 32, overlap: 10 } }\n```\n\n## Visual Elements\n\n```javascript\n{ image: true }                             // Default size 200x150\n{ image: { height: 200 } }                 // Fills available width by default\n{ image: { width: 300, height: 200 } }     // Optional constrained size\n\n{ avatar: 48 }                              // Circular, 48px\n\n{ icon: \"star\" }                            // Lucide icon name\n{ icon: \"arrow-right\", size: 32 }\n{ icon: \"brain\", size: 40, color: \"primary\" }\n\n{ badge: \"New\" }\n{ badge: \"Error\", variant: \"outline\" }\n\n{ divider: true }                           // Horizontal line\n{ divider: { orientation: \"horizontal\" } } // Fills available width by default\n{ divider: { orientation: \"horizontal\", width: 320 } } // Optional constrained width\n{ divider: { orientation: \"vertical\", height: 120 } }\n\n{ callout: \"Important information\" }\n{ callout: \"Warning message\", variant: \"warning\" }  // info, warning, error, success\n\n{ tooltip: \"Helpful hint\" }\n\n{ rectangle: true }                         // 150x100 default\n{ rectangle: { height: 100, fill: \"#f0f0f0\" } }       // Fills available width by default\n{ rectangle: { width: 200, height: 100, fill: \"#f0f0f0\" } } // Optional constrained width\n\n{ skeleton: \"text\" }                        // Presets: text, avatar, card\n{ skeleton: { height: 20 } }               // Fills available width by default\n{ skeleton: { width: 200, height: 20 } }\n\n{ spinner: true }                           // Loader icon\n{ spinner: { size: 32 } }\n```\n\nDo not nest icon options inside the icon value.\nValid:\n```javascript\n{ icon: \"brain\", size: 40, color: \"primary\" }\n```\nInvalid:\n```javascript\n{ icon: { icon: \"brain\", size: 40, color: \"primary\" } }\n```\n"}]}
